From 86d42057b9796efb3733720548dfd8c6f5597f38 Mon Sep 17 00:00:00 2001
From: Steve Wainstead <wainstead@gmail.com>
Date: Tue, 14 Jun 2011 04:02:29 +0000
Subject: [PATCH 151/223] README file added to explain this directory and the
 tools it contains.

---
 server/tools/README    |   15 +++++++++++++++
 server/tools/chroot.sh |   35 +++++++++++++++++++----------------
 2 files changed, 34 insertions(+), 16 deletions(-)
 create mode 100644 server/tools/README

diff --git a/server/tools/README b/server/tools/README
new file mode 100644
index 0000000..67f96b0
--- /dev/null
+++ b/server/tools/README
@@ -0,0 +1,15 @@
+This directory ('tools') contains scripts found around the internet by
+different authors, or written for the Waverous project.
+
+chroot.sh: This shows the basics on how to create a chroot environment
+(http://en.wikipedia.org/wiki/Chroot) to run your server, for security
+reasons
+
+moo2xml.py: convert your MOO database file to XML
+
+moobrowser.c: given different command line arguments, this tool will
+display data from your MOO database
+
+test.exp: an experimental script to use Expect
+(http://www.nist.gov/el/msid/expect.cfm) to test a running server
+
diff --git a/server/tools/chroot.sh b/server/tools/chroot.sh
index 44d2fac..e08e5a4 100755
--- a/server/tools/chroot.sh
+++ b/server/tools/chroot.sh
@@ -5,7 +5,10 @@
 MOOBIN=~swain/public_html/projects/waverous/server
 
 # path to the chroot directory you will use
-CHROOTDIR=/chroot/waverous
+CHROOTDIR=/waverous/chroot
+
+echo "To remove the chrooted environment:"
+echo "rm -rf $CHROOTDIR"
 
 mkdir -p $CHROOTDIR
 cd $CHROOTDIR
@@ -17,25 +20,25 @@ cp $MOOBIN/databases/* $CHROOTDIR/databases/
 
 # for the moo binary: run 'ldd moo' to see the list of libraries it
 # needs, then copy them to $CHROOTDIR...
-cp /lib/libexpat.so.1  lib/
-cp /lib/libcrypt.so.1  lib/
-cp /lib/libm.so.6      lib/
-cp /lib/libgcc_s.so.1  lib/
-cp /lib/libc.so.6      lib/
-cp /lib/libfreebl3.so  lib/
-cp /lib/ld-linux.so.2  lib/
-cp /lib/libdl.so.2     lib/
+cp /lib/ld-linux.so.2      lib/
+cp /lib/libc.so.6          lib/
+cp /lib/libcrypt.so.1      lib/
+cp /lib/libgcc_s.so.1      lib/
+cp /lib/libm.so.6          lib/
+cp /usr/lib/libexpat.so.0  usr/lib/
+cp /usr/lib/libstdc++.so.6 usr/lib/
 
-cp /usr/lib/libstdc++.so.6 usr/lib
 
 # for bash, if you want to run restart.sh...
-cp /lib/libtinfo.so.5 lib/
-cp /bin/bash bin/
-cd bin
-chmod a+x bash
-ln -s bash sh
-cd ..
+# cp /lib/libtinfo.so.5 lib/
+# cp /bin/bash bin/
+# cd bin
+# chmod a+x bash
+# ln -s bash sh
+# cd ..
 
 # running the server; but running it via restart.sh would be better,
 # because your changes are lost otherwise
 #chroot $CHROOTDIR /sbin/moo -l /logs/my.log /databases/LambdaCore-12Apr99.db /databases/LambdaCore-12Apr99.mine.db
+# on slim:
+# /usr/sbin/chroot /waverous/chroot /sbin/moo -l /logs/my.log /databases/LambdaCore-12Apr99.db /databases/LambdaCore-12Apr99.mine.db
\ No newline at end of file
-- 
1.7.9.5

